{
  "name": "agent-skill-harbor-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "collect": "harbor collect",
    "build": "harbor build",
    "deploy:cloudflare": "harbor deploy cloudflare-pages",
    "dev": "harbor dev",
    "preview": "harbor preview"
  },
  "dependencies": {
    "agent-skill-harbor": "^0.10.3"
  }
}
