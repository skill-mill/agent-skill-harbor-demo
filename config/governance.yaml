# Skill governance policies
# Each key is a skill path (github.com/{org}/{repo}/{skillPath})
# Supported usage_policy values: recommended, discouraged, prohibited, none
policies:
  github.com/example-org/internal-tools/skills/manual-db-migration/SKILL.md:
    usage_policy: prohibited
    note: "Use the migration framework instead of raw SQL"
  github.com/anthropics/skills/skills/frontend-design/SKILL.md:
    usage_policy: recommended
    note: "Approved for production use"
