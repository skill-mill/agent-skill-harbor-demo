name: CollectSkills

on:
  # schedule:
  #   - cron: '0 12 * * 0'  # Weekly Sun 12:00 UTC / 21:00 JST / 07:00 EST / 04:00 PST / 13:00 CET
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect_skills:
    uses: skill-mill/agent-skill-harbor/.github/workflows/collect.yml@wf-v0
    with:
      node_version: '24'
      pnpm_version: '10'
      python_version: '3.13'
    secrets: inherit
